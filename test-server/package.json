{
  "name": "express-tdd-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests/**/*.test.js --timeout 45000 --exit --slow 10000",
    "test:watch": "mocha tests/**/*.test.js --timeout 45000 --watch --slow 10000",
    "test:fast": "mocha tests/**/*.test.js --timeout 15000 --exit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  },
  "type": "module",
  "devDependencies": {
    "chai": "^5.2.0",
    "chai-http": "^5.1.2",
    "mocha": "^11.5.0"
  }
}